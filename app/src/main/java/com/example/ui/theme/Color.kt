package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ============================================
// Professional "Emerald & Brass" Culinary Palette
// Design Philosophy: Atmospheric, clear, elegant.
// Avoids template "AI Purple/Grey" aesthetics.
// ============================================

// Light Palette Colors
val LightPrimary = Color(0xFF0F5243)       // Deep Emerald Green
val LightOnPrimary = Color(0xFFFFFFFF)     // Clean white
val LightPrimaryContainer = Color(0xFFC7EADB) // Richer mint container for high contrast
val LightOnPrimaryContainer = Color(0xFF00221A) // Dark accent

val LightSecondary = Color(0xFF7E6000)     // Warm brass/gold
val LightOnSecondary = Color(0xFFFFFFFF)
val LightSecondaryContainer = Color(0xFFFFF1C5) // Richer warm brass/gold container
val LightOnSecondaryContainer = Color(0xFF382900)

val LightBackground = Color(0xFFF0F3F1)    // Sage-tinted off-white background (makes crisp white cards pop!)
val LightOnBackground = Color(0xFF131716)   // Punchier dark color for highest legibility
val LightSurface = Color(0xFFFFFFFF)       // Crisp layered surface (pure white card background)
val LightOnSurface = Color(0xFF131716)      // Crisp dark text for extreme readability
val LightSurfaceVariant = Color(0xFFDCE3DF) // Slightly darker divider/unselected container for better contrast
val LightOnSurfaceVariant = Color(0xFF323B37) // Much richer content color to satisfy contrast guidelines
val LightOutline = Color(0xFF5E6763)        // Darker outline style for clearer text borders

// Dark Palette Colors
val DarkPrimary = Color(0xFF56DCA9)        // Neon energetic Mint/Emerald
val DarkOnPrimary = Color(0xFF003828)
val DarkPrimaryContainer = Color(0xFF00513B)
val DarkOnPrimaryContainer = Color(0xFF75F9C3)

val DarkSecondary = Color(0xFFF7C324)      // Rich glowing gold
val DarkOnSecondary = Color(0xFF3B2F00)
val DarkSecondaryContainer = Color(0xFF554400)
val DarkOnSecondaryContainer = Color(0xFFFFE18F)

val DarkBackground = Color(0xFF0D1412)     // Atmospheric dark green-grey (Very high-end)
val DarkOnBackground = Color(0xFFE1E3E0)
val DarkSurface = Color(0xFF131B19)        // Rich dark slate surface
val DarkOnSurface = Color(0xFFE1E3E0)
val DarkSurfaceVariant = Color(0xFF404945)
val DarkOnSurfaceVariant = Color(0xFFBFC9C4)
val DarkOutline = Color(0xFF89938F)
